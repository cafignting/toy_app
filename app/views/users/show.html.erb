<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>
<strong>Microposts</strong>
<%= @user.microposts.each do |mic| %>
<tr>
	<td><%= @user.microposts.content %></td>
</tr>
<%= link_to 'Edit', edit_user_path(@user) %> |<%= link_to 'Back', users_path %>
